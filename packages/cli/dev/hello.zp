;; Loops a list a symbols and
;; buys one share
(import "core/trading")

(def symbols [
  "AAPL",
  "MSFT"
])

(def getStats [] => (get "stats" (portfolio)))

(loop symbol in symbols
  (buy {symbol} 1 {target: true})
)

(myObj/hello)
(date/getDay)